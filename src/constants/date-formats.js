export const FORMAT_US = 'ddd MMM D YYYY';
export const FORMAT_EU = 'ddd D MMM YYYY';
export const FORMAT_CN = 'ddd YYYY MMM D';
export const FORMAT_DEFAULT = FORMAT_US;

const FORMATS = [
  FORMAT_US,
  FORMAT_EU,
  FORMAT_CN,
];
export default FORMATS;

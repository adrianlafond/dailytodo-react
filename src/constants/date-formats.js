export const FORMAT_US = 'MMM D YYYY';
export const FORMAT_EU = 'D MMM YYYY';
export const FORMAT_CN = 'YYYY MMM D';
export const FORMAT_DEFAULT = FORMAT_US;

const FORMATS = [
  FORMAT_US,
  FORMAT_EU,
  FORMAT_CN,
];
export default FORMATS;
